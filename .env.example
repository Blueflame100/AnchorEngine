# xAI API key for Grok (get one at https://console.x.ai/)
GROK_API_KEY=

# For Docker: set to false and add GROK_API_KEY to use real Grok
# USE_MOCK_LLM=false

# Optional: override where domain YAML configs are loaded from (default: repo configs/)
# CONFIGS_DIR=/path/to/your/configs
